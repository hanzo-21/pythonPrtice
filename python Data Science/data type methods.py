# Replace ___ with your code
import numpy as np

# create a numpy array
numbers = np.array([3.3, 4.1, 5.0, 8.4, 10.1, 12.0])

# use dtype to find the data type of an array
print(numbers.dtype)

# use size to find the total number of elements in an array
print(numbers.size)